# 파이썬 입문 4주차 정리

---

## 3장 내용

### 사용자 입력으로 연산 실행

```python
# 사용자에게 입력 받은 두 개의 정수를 변수로 저장해서 연산 실행
p = int(input("첫 번째 정수를 입력하세요: "))
q = int(input("두 번째 정수를 입력하세요 (첫 번째 정수보다 작은 값): "))

print(p, "+", q, "=", p + q)
print(p, "-", q, "=", p - q)
print(p, "*", q, "=", p * q)
print(p, "//", q, "=", p // q)
print(p, "/", q, "=", p / q)
print(p, "%", q, "=", p % q)
```

---

### 산술 연산자

| 연산자 | 기호 | 사용 예시 | 결과값 |
|--------|------|------------|---------|
| 덧셈 | `+` | `7 + 4` | `11` |
| 뺄셈 | `-` | `7 - 4` | `3` |
| 곱셈 | `*` | `7 * 4` | `28` |
| 나눗셈(몫) | `//` | `7 // 4` | `1` |
| 나눗셈(실수) | `/` | `7 / 4` | `1.75` |
| 나머지 | `%` | `7 % 4` | `3` |

---

### 입력과 형변환

- 입력 함수: `input()`
- 정수로 변환 시: `int(input())`
- 오른쪽 값을 왼쪽 변수에 저장
- 코드 오류를 직접 찾아 수정할 수 있어야 함

```python
x = int(input("정수를 입력하세요: "))
print(x)
```

---

### 여러 변수에 값 저장하기

파이썬에서는 **한 줄에 여러 변수에 값 할당 가능**

```python
x, y = 100, 200
```

> 다른 언어(C, Java 등)는 `a = 3; b = 4;` 처럼 나누어 작성해야 함.  
> 파이썬은 **변수 선언 불필요** (인터프리터 방식)

---

### 증감 연산

```python
a += 1  # 가능
a = a + 1  # 가능
a++  # 불가능 (파이썬 문법 아님)
```

> 파이썬은 인터프리터 언어이므로 `a++` 같은 문법이 존재하지 않음.

---

### 관계 연산자

> 관계 연산의 결과는 **True 또는 False**

| 연산자 | 의미 |
|--------|------|
| `<`, `<=`, `>`, `>=` | 크기 비교 |
| `==`, `!=` | 같음 / 다름 비교 |

**실수(float)** 끼리의 비교는 주의 필요  
- 실수는 **근사값**으로 저장되므로 정확히 같지 않을 수 있음  
- 차이가 매우 작을 때는 같다고 판단해야 함  

---

### 연산자 우선순위

1. **괄호 `( )`**
2. **지수 연산자 `**`**
3. **단항 연산자** (`+x`, `-x`, `~x`)
4. **곱셈/나눗셈 계열** (`*`, `/`, `//`, `%`)
5. **덧셈/뺄셈** (`+`, `-`)
6. **비트 연산자** (`<<`, `>>`, `&`, `^`, `|`)
7. **비교 연산자** (`<`, `<=`, `>`, `>=`, `==`, `!=`, `is`, `is not`, `in`, `not in`)
8. **논리 연산자** (`not`, `and`, `or`)
9. **대입 연산자(할당)** (`=`, `+=`, `-=`, `*=`, `/=`, `//=`, `%=`, `**=`, `&=`, `|=`, `^=`, `>>=`, `<<=`)
10. **쉼표(,)** — 여러 값 나열 시 사용

---

### 파이썬 특징 3가지

1. **간결하고 읽기 쉬운 문법**  
   - 중괄호 `{}` 대신 **들여쓰기**로 코드 블록 구분  
   - 영어 문장처럼 읽히는 문법 → 초보자 친화적  
2. **인터프리터 언어**  
   - 한 줄씩 바로 실행  
   - 빠른 개발 및 디버깅 가능  
3. **풍부한 라이브러리와 호환성**  
   - 데이터 분석, AI, 웹 개발 등 다양한 분야 지원  
   - 오픈소스로 전 세계 개발자들이 함께 발전

---

### 랜덤 함수

```python
import random as r

print(r.randint(1, 9))   # 1~9 사이 정수
print(r.randint(1, 100)) # 1~100 사이 정수
```

---

## 4장 내용

### 파이썬의 유연함

> **자료형이 자동으로 바뀜**

---

### 따옴표 사용하기

```python
# 큰따옴표로 문자열 만들기
string1 = "Hello, world!"

# 작은따옴표로 문자열 만들기
string2 = '파이썬은 재미있어요.'
```

> 큰따옴표(`"`)와 작은따옴표(`'`)는 기능이 같으며,  
> 한 가지 스타일을 **일관성 있게** 사용하는 것이 좋음.

---

### 문자열 안에 따옴표 포함

```python
# 문자열 안에 작은따옴표 포함
message1 = "Isn't it a great day?"

# 문자열 안에 큰따옴표 포함
message2 = 'Tom said, "Hello!"'
```

---

### 자료형에 따른 연산 차이

```python
# 정수형(int)
num1 = 10
num2 = 5
print(f"정수 덧셈 결과: {num1 + num2}")  # 15

# 문자열형(str)
str1 = "10"
str2 = "5"
print(f"문자열 덧셈 결과: {str1 + str2}")  # 105
```

> 자료형이 다르면 연산 결과도 달라짐

---

### 자료형 확인하기

```python
number_int = 10
print(f"변수 {number_int}의 자료형은: {type(number_int)}")
# 출력: 변수 10의 자료형은: <class 'int'>
```

---

### 문자열 + 숫자 (오류 발생)

```python
name = "제 이름은 이가윤이고, 나이는 "
age = 20

# print(name + age)  # X TypeError 발생
```

---

### 문자열 변환 후 더하기

```python
name = "제 이름은 이가윤이고, 나이는 "
age = 20

result = name + str(age)
print(result)
```

---

### 문자열 연산

| 연산자 | 기호 | 예시 | 결과값 |
|--------|------|------|--------|
| 덧셈 | `+` | `str1 + str2` | `Hello world` |
| 곱셈 | `*` | `str1 * 3` | `안녕안녕안녕` |

---

### f-string (문자열에 변수 포함)

```python
name = "가윤"
age = 20
print(f"제 이름은 {name}이고, 나이는 {age}살입니다.")
```

---

### 리스트(List)

```python
# 여러 값을 담은 리스트 만들기
numbers = [1, 2, 3, 4, 5]
fruits = ["사과", "바나나", "딸기"]
mixed_list = [10, "Hello", 3.14, True]
```

> 리스트는 **0번 인덱스부터 시작**  
> 다른 언어의 배열처럼 복잡한 포인터를 쓸 필요 없이  
> **간단한 문법**으로 여러 데이터를 저장 가능

---

### 특수 문자열

| 표현 | 의미 |
|------|------|
| `\n` | 줄바꿈 (Newline) |
| `\t` | 탭 (Tab) |
| `\\` | 백슬래시 (Backslash) |
| `\'` | 작은따옴표 (Single quote) |
| `\"` | 큰따옴표 (Double quote) |

---

## 정리

- 파이썬은 **간결하고 직관적인 문법**
- 변수 선언 없이 바로 사용 가능
- 문자열, 리스트, 형변환, 연산자 개념 필수
- `input()`, `print()`, `f-string`, `type()` 적극 활용
