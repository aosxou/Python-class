# 파이썬 입문 5주차 정리

---

## 5장 내용 정리

### 3가지 기본 제어 구조

1. **순차 구조 (Sequential Structure)**  
   → 명령들이 **순서대로 실행**되는 구조  

2. **선택 구조 (Selection Structure)**  
   → **둘 중 하나의 명령을 선택**하여 실행하는 구조  

3. **반복 구조 (Repetition Structure)**  
   → **같은 명령을 여러 번 반복** 실행하는 구조  

---

## if-else문

> 조건이 참이면 **A를 실행**하고, 거짓이면 **B를 실행**하는 구조

```python
if score >= 60:
    print("합격입니다.")
else:
    print("불합격입니다.")
```

- 조건식은 일반적으로 `"score >= 60"` 같은 **비교식**
- 조건식 뒤에는 반드시 **콜론(`:`)** 을 붙여야 함  
  → 콜론은 **“문장이 아직 끝나지 않았다”**는 의미
- **들여쓰기(Indentation)** 는 **4칸(space 4개)** 권장  
  (들여쓰기 불일치 시 오류 발생)
- `else` 문은 **생략 가능**

> 시험 포인트  
> - 들여쓰기 칸 안 맞으면 오류 발생  
> - 콜론(`:`) 절대 빼먹지 말 것  

---

## 관계 연산자 (Relational Operators)

| 연산자 | 의미 | 예시 | 설명 |
|--------|------|------|------|
| `==` | 같다 | `x == y` | x와 y가 같은가? |
| `!=` | 다르다 | `x != y` | x와 y가 다른가? |
| `>` | 크다 | `x > y` | x가 y보다 큰가? |
| `<` | 작다 | `x < y` | x가 y보다 작은가? |
| `>=` | 크거나 같다 | `x >= y` | x가 y보다 크거나 같은가? |
| `<=` | 작거나 같다 | `x <= y` | x가 y보다 작거나 같은가? |

---

## 논리 연산자 (Logical Operators)

| 연산자 | 의미 | 설명 |
|--------|------|------|
| `and` | 논리 AND | x와 y가 **모두 참**이면 참 |
| `or` | 논리 OR | x와 y 중 **하나라도 참**이면 참 |
| `not` | 논리 NOT | x가 참이면 거짓, x가 거짓이면 참 |

```python
x = True
y = False

print(x and y)  # False
print(x or y)   # True
print(not x)    # False
```

---

## 6장 내용 정리

### 반복문의 종류

1. **횟수 제어 반복 (for문)**  
   → **정해진 횟수만큼 반복**
   ```python
   for i in range(5):
       print(i)
   # 출력: 0 1 2 3 4
   ```

2. **조건 제어 반복 (while문)**  
   → **특정 조건이 참일 동안 반복**
   ```python
   count = 0
   while count < 3:
       print("반복 중...")
       count += 1
   ```

---

## range() 함수

> 연속적인 숫자들을 만들어내는 함수

| 사용 예시 | 생성 결과 | 설명 |
|------------|------------|------|
| `range(5)` | 0, 1, 2, 3, 4 | 기본 형태 (0부터 시작, 1씩 증가) |
| `range(3, 10, 2)` | 3, 5, 7, 9 | 3부터 10 전까지 2씩 증가 |

- 기본값:  
  - `start` (시작값) → 0  
  - `step` (증가값) → 1  
- **시험 출제 자주됨** ⚡

---

## 🐢 t.done() 사용하는 이유

> **거북이 그래픽(turtle)** 프로그램이  
> 실행 직후 창이 닫히지 않도록 유지하기 위해 사용함.

```python
import turtle as t

t.circle(50)
t.done()  # 그림이 그려진 후 창을 바로 닫지 않게 유지
```

→ **그림을 다 그리고 나서 멈춰두는 역할**

---

## 🧾 핵심 정리

- 제어 구조는 **순차 / 선택 / 반복** 3가지
- `if-else` 문에서는 **콜론(`:`)** 과 **들여쓰기** 주의
- **관계 / 논리 연산자**의 의미 정확히 구분
- **for, while** 반복문은 각각 **횟수 제어 / 조건 제어**
- **range()** 함수와 **t.done()**은 시험 단골 주제

---
