# 파이썬 입문 3주차 정리

---

## 변수(Variable)

> **외부로부터 들어오는 값을 저장하는 공간**  
> 변수는 **메모리상에 만들어지며**, 프로그램이 실행될 때 `*.exe` 실행 파일 내에 생성됨.

---

## 파이썬 변수 생성 방법

- 파이썬은 **인터프리터 방식**이기 때문에 **선언문이 없음**  
  → `변수명 = 값` 형태로 작성  
  ```python
  x = 100
  ```
- 변수에는 **정수형뿐 아니라 문자열도 저장 가능**
  ```python
  name = "Python"
  ```
- **의미 있는 변수명** 사용 권장
- 변수명 **중간에 공백 불가**
- **숫자나 특수기호**는 변수명에 사용할 수 없음  
  단, **언더바(`_`)는 맨 앞이나 중간에 사용 가능**
- **소문자와 대문자를 구분함**
  ```python
  age = 10
  Age = 20   # 다른 변수로 인식됨
  ```

---

## 대입 연산자(Assignment Operator)

| 연산자 | 설명 | 예시 |
|--------|------|------|
| `=` | 오른쪽 값을 왼쪽 변수에 저장 | `x = 5` |
| `+=` | 기존 값에 더함 | `x += 1` → `x = x + 1` |
| `-=` | 기존 값에서 뺌 | `x -= 1` → `x = x - 1` |

> **C언어와 달리**  
> - 세미콜론(`;`) 사용 X  
> - 증감 연산자(`++`, `--`) X  

---

## 🧑‍💻 사용자 입력 함수

> **사용자로부터 입력을 받는 함수:** `input()`

- 기본 사용
  ```python
  x = input("내용 : ")
  print(x)
  ```
- **정수형 입력**을 받을 때는 `int()`로 형 변환
  ```python
  num = int(input("정수를 입력하세요: "))
  print(num + 10)
  ```

> 참고:  
> 컴퓨터 그래픽 장면 역시 내부적으로는 **컴퓨터의 계산 기능**을 통해 만들어짐.

---

## 파이썬에만 있는 연산자

| 연산자 | 설명 | 예시 | 결과 |
|--------|------|------|------|
| `//` | 나눗셈 후 **몫**만 반환 | `7 // 3` | `2` |
| `%` | 나눗셈 후 **나머지**만 반환 | `7 % 3` | `1` |

---

## 정리

- 변수는 **데이터를 저장하는 메모리 공간**
- 파이썬 변수는 **타입 선언 없이** 사용 가능
- **대입 연산자**는 `=`이며, **축약형**(`+=`, `-=` 등) 지원
- **입력 함수**: `input()`
- **고유한 연산자**: `//`, `%`

---
