# 파이썬 입문 6주차 정리

---

## 6장 이론 정리

### 반복의 종류

| 반복 구조 | 설명 | 예시 상황 |
|------------|------|------------|
| **횟수 제어 반복 (for문)** | 정해진 횟수만큼 반복 | 반복 횟수를 **알 때** 사용 |
| **조건 제어 반복 (while문)** | 특정 조건이 만족될 때까지 반복 | 반복 횟수를 **모를 때** 사용 |

---

## for문 (횟수 제어 반복)

> 정해진 횟수만큼 반복 수행할 때 사용

```python
for i in [1, 2, 3, 4, 5]:
    print("방문을 환영합니다.")
```

- 반복문 끝에는 반드시 **콜론(`:`)** 을 붙여야 함  
- 반복되는 문장은 반드시 **들여쓰기** 해야 함  
- 위의 예제는 `i`가 1부터 5까지 변화하며 반복 실행됨  

---

## 숫자의 합 구하기 예제

```python
sum = 0
for i in [1, 2, 3, 4, 5]:
    print(f"{i}를 더하는 중")
    sum += i

print("합계는 =", sum)
```

**출력 결과:**
```
1를 더하는 중
2를 더하는 중
3를 더하는 중
4를 더하는 중
5를 더하는 중
합계는 = 15
```

---

## for문의 기본 문법

```python
for 변수 in range(종료값):
    반복할 문장
```

- `range(종료값)` → **0부터 종료값-1까지** 숫자를 생성
- 예: `range(5)` → 0, 1, 2, 3, 4  

---

## range() 함수 상세 구조

```python
range(start=0, stop, step=1)
```

| 인자 | 의미 | 설명 |
|------|------|------|
| `start` | 시작값 | 생략 시 0부터 시작 |
| `stop` | 종료값 | **해당 값은 포함되지 않음** |
| `step` | 증가 단위 | 기본값은 1 |

예제   
```python
for i in range(1, 6, 1):
    print(i, end=" ")
```

**출력 결과:**  
```
1 2 3 4 5
```

---

## while문 (조건 제어 반복)

> 특정 조건이 참인 동안 반복 실행

```python
while 조건:
    반복할 문장
```

- 조건이 **참(True)** 인 동안 계속 실행  
- 조건이 **거짓(False)** 가 되면 반복 종료  

---

### 무한 루프와 `break` 예제

> 조건이 복잡하거나 종료 시점을 알 수 없을 때  
> `while True:` 로 무한 반복을 만들고 `break`로 탈출함

```python
while True:
    light = input("신호등 색상을 입력하세요: ")
    if light == "blue":
        break
    print("전진!!")
```

**실행 예시:**
```
신호등 색상을 입력하세요: red
전진!!
신호등 색상을 입력하세요: yellow
전진!!
신호등 색상을 입력하세요: blue
# 반복 종료
```

---

## 🧾 핵심 요약

- 반복문에는 **for문(횟수 제어)** 과 **while문(조건 제어)** 가 있음  
- `for`문에서는 **range()** 함수 자주 사용  
- 반복문 뒤에는 항상 **콜론(`:`)** 필수  
- 들여쓰기(indent) 불일치 시 오류 발생  
- `while True` + `break`로 **무한 루프 제어 가능**

---
