# 파이썬 입문 2주차 정리

---

## 파이썬이 인기 있는 이유

1. **간결하고 읽기 쉬운 구문**, **유용한 라이브러리**, **쉬운 유지보수성** 덕분에 인기가 많다.  
   → *인터프리터 방식*으로 동작하며, **소스 코드만 관리하면 됨**.
2. **웹 개발**, **시스템 자동화**, **데이터 처리**, **인공지능(AI)** 등 다양한 분야에서 사용됨.
3. 문법이 쉬워서 **초보 개발자들도 접근하기 쉬움**.

---

## 파이썬의 특징

- 생산성이 뛰어남  
- 초보자에게 친숙함  
- **인터프리터 언어**  
  → 코드를 한 줄씩 해석하여 실행함

---

## 주요 파이썬 라이브러리

| 분야 | 라이브러리 이름 |
|------|----------------|
| GUI | `Tkinter` |
| Web Framework | `Django` |
| Game Development | `pygame` |
| Molecular Biology | `Biopython` |
| Natural Language Processing | `nltk` |
| Data Visualization | `matplotlib` |
| Scientific Computing | `numpy` |

> **라이브러리란?**  
> 자주 사용할 **함수(function)** 들을 미리 만들어 모아둔 것.

---

## 함수(Function)

### 함수 정의
> 함수란 **한 가지 기능을 수행하는 코드 블록**을 의미한다.

- 특정 기능을 수행하도록 **프로그램 언어로 설명한 부분**
- 예: "원하는 부분에 사각형을 그리는 기능"을 코드로 표현한 것

```python
def draw_square():
    print("사각형을 그립니다.")
```

### 함수 호출
> 정의된 함수를 **불러와 실행**하는 것

```python
draw_square()
```

---

## 변수(Variable)

> **데이터를 저장하기 위한 공간**  
> 외부로부터 들어오는 값을 임시로 보관하기 위해 필요함

### 변수의 특징
- 파이썬은 **컴파일형이 아닌 인터프리터형 언어**
- 변수 선언 시 **자료형(type)** 을 미리 지정할 필요 없음  
  → 값이 정수면 자동으로 정수형 메모리가 할당됨
- 파일 확장자는 `.py`

### 변수 이름 규칙
1. **의미 있는 이름** 사용  
2. **소문자와 대문자는 구분됨** (`score` ≠ `Score`)
3. **영문자, 숫자, 밑줄(`_`)** 로 구성 가능  
4. **공백 불가**, 여러 단어는 `_` 로 구분 (`student_name`)

---

## C언어 / Java 와의 차이점

| 구분 | C / Java | Python |
|------|-----------|--------|
| 변수 선언 | 타입 지정 필요 | 타입 지정 불필요 |
| 문장 끝 구분 | 세미콜론(`;`) 필요 | 세미콜론 생략 가능 |
| 실행 방식 | 컴파일 방식 | 인터프리터 방식 |

---

## 정리

- 파이썬은 읽기 쉽고 간결한 **고급 언어**  
- 다양한 **라이브러리** 제공으로 생산성이 높음  
- **변수, 함수, 라이브러리** 개념을 이해하는 것이 핵심
